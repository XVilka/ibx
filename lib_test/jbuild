(executables
 ((names (test_runner))
  (libraries (async
              core
              ibx
              textutils))
  (preprocess (pps (ppx_sexp_conv
                    ppx_fields_conv)))))

(jbuild_version 1)
